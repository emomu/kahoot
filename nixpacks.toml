[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.install]
cmds = [
  "cd server && npm ci --omit=dev",
  "cd client && npm ci && npm run build"
]

[phases.build]
cmds = []

[start]
cmd = "cd server && npm start"
